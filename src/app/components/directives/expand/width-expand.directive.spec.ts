import { WidthExpandDirective } from './width-expand.directive';

describe('WidthExpandDirective', () => {
  it('should create an instance', () => {
    const directive = new WidthExpandDirective();
    expect(directive).toBeTruthy();
  });
});
